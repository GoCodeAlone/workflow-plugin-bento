# Minimal config exercising workflow-plugin-bento.
# Schema-validate with: wfctl validate --skip-unknown-types examples/minimal/config.yaml

version: 1

modules:
  - name: bento-provider
    type: bento.stream
    config: {}

workflows:
  pipeline:
    steps:
      - name: example
        type: step.bento
        config: {}
